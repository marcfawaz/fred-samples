# Academy Servers

Reusable demo components live here, grouped by protocol and language.

## MCP Servers (Python)

- `fred-academy/servers/mcp/python/minimal-mcp-server`
- `fred-academy/servers/mcp/python/postal-service-mcp-server`
- `fred-academy/servers/mcp/python/iot-tracking-mcp-server`

Each folder contains its own `README` and `Makefile`.

These are demo/sample implementations (not production services).
